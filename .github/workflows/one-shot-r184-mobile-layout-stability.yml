name: One-shot final pass bridge

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/one-shot-r184-mobile-layout-stability.yml'

permissions:
  contents: write

jobs:
  verify-bridge:
    runs-on: ubuntu-latest
    steps:
      - name: Verify bridge
        run: echo r382-bridge-ready
