name: Verify R482 live MAG semantic replacement
run-name: R482 semantic replacement for ${{ github.event.workflow_run.head_sha || github.sha }}

on:
  workflow_run:
    workflows: ["Prepare or deploy FormatX production"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

concurrency:
  group: formatx-r482-semantic-${{ github.event.workflow_run.head_sha || github.sha }}
  cancel-in-progress: false

jobs:
  live-r482-mag:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success' &&
       contains(github.event.workflow_run.head_commit.message, '[deploy-production]'))
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      AUDITED_SHA: ${{ github.event.workflow_run.head_sha || github.sha }}
      FORMATX_TEST_URL: https://formatxsuite.com/
      FORMATX_SEMANTIC_EVIDENCE_DIR: artifacts/r482-semantic-replacement
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ env.AUDITED_SHA }}

      - name: Wait for exact deployed P0 marker
        shell: bash
        run: |
          set -euo pipefail
          for attempt in $(seq 1 60); do
            if curl -fsS --max-time 20 -H 'Cache-Control: no-cache' \
              "https://formatxsuite.com/p0-validation-version.txt?r482_wait=${GITHUB_RUN_ID}_$attempt" \
              -o "$RUNNER_TEMP/live-p0.txt" \
              && cmp -s docs/p0-validation-version.txt "$RUNNER_TEMP/live-p0.txt"; then
              echo 'Exact production revision is live.'
              exit 0
            fi
            sleep 5
          done
          echo 'Exact production marker did not converge.' >&2
          exit 1

      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: billing-worker/package-lock.json

      - name: Validate semantic replacement source
        run: node --check .github/scripts/validate-r522-semantic-mag.cjs

      - name: Reuse system Chrome
        shell: bash
        run: |
          set -euo pipefail
          npm install --no-save --no-package-lock --no-audit --no-fund playwright@1.62.0
          CHROME_BIN="$(command -v google-chrome || command -v google-chrome-stable || command -v chromium || command -v chromium-browser)"
          test -n "$CHROME_BIN"
          echo "CHROME_BIN=$CHROME_BIN" >> "$GITHUB_ENV"

      - name: Verify current mobile and desktop MAG semantics
        run: node .github/scripts/validate-r522-semantic-mag.cjs

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: r482-semantic-replacement-${{ env.AUDITED_SHA }}
          path: artifacts/r482-semantic-replacement/
          if-no-files-found: warn
          retention-days: 30

      - name: Publish R482 semantic replacement status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          JOB_STATUS: ${{ job.status }}
        shell: bash
        run: |
          if [ "$JOB_STATUS" = success ]; then
            state=success
            description='R482 legacy gate replaced by current bounded MAG semantics'
          else
            state=failure
            description='R482 semantic replacement failed'
          fi
          gh api --method POST "repos/$GITHUB_REPOSITORY/statuses/$AUDITED_SHA" \
            -f state="$state" \
            -f context='FormatX R482 live MAG' \
            -f description="$description" \
            -f target_url="https://github.com/$GITHUB_REPOSITORY/actions/runs/$GITHUB_RUN_ID"
