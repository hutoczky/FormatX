name: Lighthouse CI

on:
  pull_request:
    branches: [master]
    paths:
      - 'docs/scifi-ui/**'
      - 'lighthouserc.json'
      - '.github/workflows/ci.yml'
  push:
    branches: [master]
    paths:
      - 'docs/scifi-ui/**'
      - 'lighthouserc.json'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lighthouse-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install -g @lhci/cli@0.13.x

      - name: Run Lighthouse CI
        id: lighthouse
        shell: bash
        run: |
          set -o pipefail
          lhci autorun --rc=lighthouserc.json 2>&1 | tee lighthouse-output.txt
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

      - name: Upload Lighthouse results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: lighthouse-results
          path: |
            .lighthouseci/
            lighthouse-output.txt
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30

      - name: Summarize Lighthouse results
        if: always()
        run: |
          {
            echo "## Lighthouse CI Results"
            echo
            echo "Production: https://www.formatxsuite.com/"
            echo
            echo "### Thresholds"
            echo "- Performance ≥ 0.90"
            echo "- Accessibility ≥ 0.95"
            echo "- Best practices ≥ 0.95"
            echo "- SEO ≥ 0.90"
            echo "- LCP < 2.5s"
            echo "- CLS < 0.10"
            echo "- TTFB < 600ms"
            echo
            echo "The production site is deployed as one Cloudflare Worker with Static Assets."
          } >> "$GITHUB_STEP_SUMMARY"
