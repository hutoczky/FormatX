name: Build and publish FormatX Android APK

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android publisher temporarily paused for the dual-site 1.0.5 source update."
