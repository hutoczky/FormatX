name: One-shot r381 bridge

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/one-shot-r160-reference-seamless-key.yml'

permissions:
  contents: write

jobs:
  verify-bridge:
    runs-on: ubuntu-latest
    steps:
      - name: Verify registered workflow
        run: echo 'r381 bridge ready'
