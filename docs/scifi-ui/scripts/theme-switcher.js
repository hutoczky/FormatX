(() => { const root = document.documentElement; const btns = Array.from(document.querySelectorAll('.switcher .theme')); const link = document.getElementById('theme-css'); const brand = document.getElementById('brand'); const KEY = 'scifi-ui.theme'; const saved = localStorage.getItem(KEY); if (saved) { apply(saved, false); } btns.forEach(b => { b.addEventListener('click', () => apply(b.dataset.theme, true)); b.addEventListener('keydown', e => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); apply(b.dataset.theme, true); } }); }); function apply(theme, persist) { if (!theme) return; root.setAttribute('data-theme', theme); if (link) { link.href = `styles/${theme}.css`; } btns.forEach(b => { const on = b.dataset.theme === theme; b.setAttribute('aria-pressed', on ? 'true' : 'false'); }); if (brand) { if (theme === 'cyberpunk') { brand.setAttribute('data-glitch', 'on'); } else { brand.removeAttribute('data-glitch'); } } if (persist) localStorage.setItem(KEY, theme); document.dispatchEvent(new CustomEvent('themechange', { detail: { theme } })); } window.setTheme = (theme) => apply(theme, true); })();