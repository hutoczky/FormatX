/* FormatX R753 — bounded OffscreenCanvas WebGL1 MAG worker.
   First frame deliberately uses a compact two-attribute shader and a bounded
   12x24 closed surface. This is the same product path on every browser: no
   audit/headless/UA branching. Expensive procedural facet lighting is retired
   from initial compilation so automatic MAG startup cannot monopolise GPU/software
   rasterisation after intro release. */
'use strict';
let gl=null,canvas=null,program=null,count=0,buffers=[],uniforms={},width=2,height=2;
let morph=0,energy=.5,breath=.12,pointerX=0,pointerY=0,rotationY=0,surfacePulse=-1;
function shader(type,source){const s=gl.createShader(type);gl.shaderSource(s,source);gl.compileShader(s);if(!gl.getShaderParameter(s,gl.COMPILE_STATUS)){const e=gl.getShaderInfoLog(s)||'shader compile failed';gl.deleteShader(s);throw new Error(e);}return s;}
function buildProgram(){const vs=shader(gl.VERTEX_SHADER,`precision mediump float; attribute vec3 aCrystal;attribute vec3 aSphere;uniform float uMorph,uAspect,uRotationY,uBreath;uniform vec2 uPointer;varying vec3 vLocal;void main(){float m=uMorph*uMorph*(3.0-2.0*uMorph);vec3 p=mix(aCrystal,aSphere,m);float c=cos(uRotationY),s=sin(uRotationY);vec3 q=vec3(c*p.x+s*p.z,p.y,-s*p.x+c*p.z);q*=1.0+uBreath*.015;q.xy+=uPointer*.02;vLocal=q;float camera=3.05-q.z*.62;vec2 xy=vec2(q.x/max(.60,uAspect),q.y)*(2.62/max(1.75,camera));gl_Position=vec4(xy,q.z*.12,1.0);}`);const fs=shader(gl.FRAGMENT_SHADER,`precision mediump float; uniform float uEnergy,uSurfacePulse;varying vec3 vLocal;void main(){float r=length(vLocal.xy);float edge=clamp(1.0-r*.68,0.0,1.0);float nucleus=pow(clamp(1.0-r/.24,0.0,1.0),3.0);float pulse=uSurfacePulse<0.0?0.0:clamp(1.0-abs((.5+vLocal.y*.5)-uSurfacePulse)*8.0,0.0,1.0);vec3 cyan=vec3(.05,.78,1.0),violet=vec3(.48,.20,.92),ice=vec3(.88,1.0,1.0);vec3 color=mix(violet,cyan,.68)+ice*(nucleus*.85+pulse*.28)+cyan*uEnergy*.16;float alpha=clamp(.28+edge*.28+nucleus*.22+pulse*.10,.24,.78);gl_FragColor=vec4(color/(vec3(1.0)+color),alpha);}`);const p=gl.createProgram();gl.attachShader(p,vs);gl.attachShader(p,fs);gl.bindAttribLocation(p,0,'aCrystal');gl.bindAttribLocation(p,1,'aSphere');gl.linkProgram(p);gl.deleteShader(vs);gl.deleteShader(fs);if(!gl.getProgramParameter(p,gl.LINK_STATUS)){const e=gl.getProgramInfoLog(p)||'program link failed';gl.deleteProgram(p);throw new Error(e);}return p;}
function geometry(){const latSeg=12,lonSeg=24,crystal=[],sphere=[];function vertex(la,lo){const phi=(la/latSeg)*Math.PI,theta=(lo/lonSeg)*Math.PI*2,s=Math.sin(phi),d=[s*Math.cos(theta),Math.cos(phi),s*Math.sin(theta)],ax=d[0]>=0?.88:.86,ay=d[1]>=0?1.09:.97,az=d[2]>=0?.64:.43,e=.78,t=Math.pow(Math.abs(d[0])/ax,e)+Math.pow(Math.abs(d[1])/ay,e)+Math.pow(Math.abs(d[2])/az,e),r=1/Math.pow(Math.max(.0001,t),1/e);return{c:d.map(x=>x*r),s:d.map(x=>x*.91)};}function tri(a,b,c){for(const q of[a,b,c]){crystal.push(...q.c);sphere.push(...q.s);}}for(let la=0;la<latSeg;la++)for(let lo=0;lo<lonSeg;lo++){const a=vertex(la,lo),b=vertex(la,lo+1),c=vertex(la+1,lo),d=vertex(la+1,lo+1);if(la>0)tri(a,b,c);if(la<latSeg-1)tri(b,d,c);}return{arrays:[new Float32Array(crystal),new Float32Array(sphere)],count:crystal.length/3};}
function upload(index,data){const b=gl.createBuffer();buffers.push(b);gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,data,gl.STATIC_DRAW);gl.enableVertexAttribArray(index);gl.vertexAttribPointer(index,3,gl.FLOAT,false,0,0);}
function resize(w,h){width=Math.max(2,w|0);height=Math.max(2,h|0);canvas.width=width;canvas.height=height;gl.viewport(0,0,width,height);}
function render(){if(!gl||!program)return;gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.useProgram(program);gl.uniform1f(uniforms.uMorph,morph);gl.uniform1f(uniforms.uAspect,width/Math.max(1,height));gl.uniform1f(uniforms.uRotationY,rotationY);gl.uniform1f(uniforms.uBreath,breath);gl.uniform2f(uniforms.uPointer,pointerX,pointerY);gl.uniform1f(uniforms.uEnergy,energy);gl.uniform1f(uniforms.uSurfacePulse,surfacePulse);gl.drawArrays(gl.TRIANGLES,0,count);gl.flush();}
function init(data){canvas=data.canvas;gl=canvas.getContext('webgl',{alpha:true,antialias:false,depth:true,stencil:false,premultipliedAlpha:false,preserveDrawingBuffer:false,powerPreference:'low-power'});if(!gl)throw new Error('offscreen webgl unavailable');program=buildProgram();const g=geometry();count=g.count;gl.useProgram(program);g.arrays.forEach((a,i)=>upload(i,a));['uMorph','uAspect','uRotationY','uBreath','uPointer','uEnergy','uSurfacePulse'].forEach(n=>uniforms[n]=gl.getUniformLocation(program,n));gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE_MINUS_SRC_ALPHA);gl.clearColor(0,0,0,0);resize(data.width,data.height);render();postMessage({type:'ready',count,context:'webgl1-offscreen',revision:'r753-bounded-first-frame'});}
onmessage=event=>{const d=event.data||{};try{if(d.type==='init'){init(d);return;}if(d.type==='resize'){resize(d.width,d.height);render();return;}if(d.type==='state'){if(Number.isFinite(d.morph))morph=Math.max(0,Math.min(1,d.morph));if(Number.isFinite(d.energy))energy=d.energy;if(Number.isFinite(d.breath))breath=d.breath;if(Number.isFinite(d.pointerX))pointerX=d.pointerX;if(Number.isFinite(d.pointerY))pointerY=d.pointerY;if(Number.isFinite(d.rotationY))rotationY=d.rotationY;if(Number.isFinite(d.surfacePulse))surfacePulse=d.surfacePulse;render();return;}if(d.type==='destroy'){buffers.forEach(b=>gl?.deleteBuffer(b));if(program)gl?.deleteProgram(program);close();}}catch(error){postMessage({type:'error',message:String(error?.message||error)});}};
