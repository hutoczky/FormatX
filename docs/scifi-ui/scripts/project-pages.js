(function(){'use strict';document.body.id='top';const sectionHashes=new Set(['#users','#benefits','#modules','#flow','#layers','#details']);if(sectionHashes.has(window.location.hash)){window.requestAnimationFrame(function(){const target=document.querySelector('.content-shell');if(target)target.scrollIntoView()})}const toggle=document.querySelector('.page-menu-toggle');const nav=document.querySelector('.page-nav');if(toggle&&nav){toggle.addEventListener('click',function(){const open=!nav.classList.contains('open');nav.classList.toggle('open',open);toggle.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',function(e){if(e.target.closest('a')){nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}});window.addEventListener('keydown',function(e){if(e.key==='Escape'){nav.classList.remove('open');toggle.setAttribute('aria-expanded','false')}})}const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const fine=window.matchMedia('(pointer:fine)').matches;if(reduce||!fine)return;function bindTilt(card,strengthX,strengthY){card.addEventListener('pointermove',function(e){const r=card.getBoundingClientRect();const px=(e.clientX-r.left)/r.width;const py=(e.clientY-r.top)/r.height;const x=px-.5;const y=py-.5;card.style.setProperty('--rx',(-y*strengthX).toFixed(2)+'deg');card.style.setProperty('--ry',(x*strengthY).toFixed(2)+'deg');card.style.setProperty('--glare-x',(px*100).toFixed(1)+'%');card.style.setProperty('--glare-y',(py*100).toFixed(1)+'%')});card.addEventListener('pointerleave',function(){card.style.removeProperty('--rx');card.style.removeProperty('--ry');card.style.removeProperty('--glare-x');card.style.removeProperty('--glare-y')})}document.querySelectorAll('.depth-card').forEach(function(card){bindTilt(card,14,18)});document.querySelectorAll('.page-link-card').forEach(function(card){bindTilt(card,10,14)});const hero=document.querySelector('.hero-visual');if(hero){window.addEventListener('pointermove',function(e){const x=e.clientX/window.innerWidth-.5;const y=e.clientY/window.innerHeight-.5;hero.style.setProperty('--hero-ry',(-15+x*14).toFixed(2)+'deg');hero.style.setProperty('--hero-rx',(7-y*10).toFixed(2)+'deg')},{passive:true});window.addEventListener('mouseleave',function(){hero.style.removeProperty('--hero-ry');hero.style.removeProperty('--hero-rx')})}})();